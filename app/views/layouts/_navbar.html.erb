<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid d-flex flex-column flex-md-row justify-content-center">
    <a class="navbar-brand" href="/" aria-label="RubyMovie">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="d-block mx-auto" role="img" viewBox="0 0 24 24"><title>RubyMovie</title><circle cx="12" cy="12" r="10"></circle><path d="M14.31 8l5.74 9.94M9.69 8h11.48M7.38 12l5.74-9.94M9.69 16L3.95 6.06M14.31 16H2.83m13.79-4l-5.74 9.94"></path></svg>
    </a>
    <a class="navbar-brand" href="/">RubyMovie</a>
       <div class="dropdown me-3">
         <a class="btn btn-outline-light dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
           Filter by Category
         </a>
         <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
           <% Category.all.each do |cat| %>
             <li>
               <%= link_to cat.name, root_path(category: cat.name), class: "dropdown-item" %>
             </li>
           <% end %>
         </ul>
       </div>
    <% if user_signed_in? %>
      <%= link_to 'Edit profile', edit_user_registration_path, class: 'btn btn-outline-warning me-3' %>
      <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger me-3' %>
      <span class="align-middle" style="color: white;">Logged in as: <%= current_user.email %>.</span>
    <% else %>
      <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-outline-light me-3' %>
      <%= link_to 'Register', new_user_registration_path, class: 'btn btn-outline-warning' %>
    <% end %>
  </div>
</nav>